import { Injectable } from '@angular/core'
import { Observable, Subject, BehaviorSubject } from 'rxjs'

import { Alert, AlertDetails } from './alert.model'

@Injectable({
  providedIn: 'root'
})
export class AlertService {
  constructor() {}
}
